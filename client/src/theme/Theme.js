import { createTheme } from '@mui/material';

const Theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#00715d',
    },
    secondary: {
      main: '#f50057',
    },
  },
});

export default Theme;